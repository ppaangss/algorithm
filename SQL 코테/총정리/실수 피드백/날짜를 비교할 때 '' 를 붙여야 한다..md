# 날짜에 '' 를 붙여야 한다.

예를들어 컬럼이 DATE 이고 2022년 11월 01일 이후 날짜만 추출해야한다.

`DATE > '2022-11-01'` 이렇게 작성해야 한다.

```sql
SELECT DATE 
FROM DATE_ALL
WHERE DATE > '2022-11-01';
```

가능한 형식들

- `DATE_FORMAT(PRODUCE_DATE, '%Y-%m') = '2022-05'` 
- `YEAR=2022 AND MONTH=5`
