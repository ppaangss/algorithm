# 서브쿼리

서브쿼리에 컬럼 2개 이상을 받아올 수 있다.

```sql
WHERE (I.FISH_TYPE, I.LENGTH) IN (
    SELECT FISH_TYPE, MAX(LENGTH)
    FROM FISH_INFO
    GROUP BY FISH_TYPE
)
```

# 예시코드
[[prog. 물고기 종류 별 대어 찾기]]

```

```